diff --git a/meson.build b/meson.build
index e713b3dc..11590ac9 100644
--- a/meson.build
+++ b/meson.build
@@ -82,8 +82,8 @@ if get_option('testmains') or get_option('python-module') or get_option('indexer
     if build_machine.system() == 'freebsd'
         libiconv = cc.find_library('libiconv', required:true, dirs: '/usr/local/lib')
         librecolldeps += libiconv
-    else 
-        librecolldeps += dependency('iconv', method : 'auto')
+#    else 
+#        librecolldeps += dependency('iconv', method : 'auto')
     endif
 else
     librecoll_needed = false
